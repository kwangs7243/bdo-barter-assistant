# BDO Barter Assistant — Product Specification v0.1

## 1. 목적

검은사막 한국 서버 물물교환을 위한 Windows 로컬 전용 도우미를 만든다.

사용자가 매 갱신마다 물물교환 목록과 재고를 수기로 다시 적지 않도록 다음 흐름을 제공하는 것이 최종 목표다.

```text
게임 물물교환 창 열기
  → 스캔 시작
  → 사용자가 목록 스크롤
  → 로컬 OCR 및 후보 매칭
  → 불확실 항목 검수
  → 로컬 재고와 v14.1 규칙으로 출항 계획 생성
  → 사용자가 게임에서 실제 수행
  → 완료 확정
  → 확정된 교환만 로컬 재고 갱신
```

## 2. 기준 자료

- 원본 참고 구현: `reference/original/물물교환 지능형 스케쥴러_배포용 v14.1_by 송도조씨.html`
- OCR 전체화면 샘플: `reference/samples/barter_fullscreen.png`
- OCR 크롭 샘플: `reference/samples/barter_cropped.png`

원본 HTML에서는 품목 사전, 섬 좌표, 항로 보정, 계산 규칙과 튜닝값을 조사한다. 기존 UI, Gemini API, API 키, 모델 선택, 제작자용 진단 화면은 새 제품의 요구사항으로 승계하지 않는다.

## 3. 사용 환경

- 대상 OS: Windows 10
- 언어: Python 3.11
- 프로젝트/의존성 관리: uv
- 기본 실행: CPU-first
- 네트워크: 실행 중 외부 AI/OCR API 호출 없음
- 최종 배포 후보: 독립 실행형 Windows 패키지. 구체 도구는 M8에서 결정한다.

## 4. 기능 요구사항

### FR-01 화면 입력

- 사용자는 검은사막 물물교환 창을 직접 연다.
- 앱은 게임 화면에서 물물교환 목록 영역을 가져온다.
- 최초 1회 영역 지정 또는 안정적인 자동 탐지 중 어느 방식을 쓸지는 M1~M2 검증 후 결정한다.
- 앱이 게임 입력을 대신하거나 목록을 자동 스크롤하지 않는다.

### FR-02 로컬 OCR

- 모든 이미지 처리와 OCR은 로컬 PC에서 실행한다.
- 특정 OCR 라이브러리는 M0에서 확정하지 않는다.
- OCR은 행의 관찰값을 섬, 남은 교환 횟수, 소모 품목, 요구 수량, 획득 품목, 획득 수량으로 구조화한다.
- 원본 Gemini 호환 계층은 모든 논리 필드를 직접 OCR할 필요가 없다. 원본 reference 규칙으로 결정 가능한 `reqAmount`와 `yield`는 `reference_rule` 출처로 결정적으로 파생할 수 있다.
- 최종 `scannedTrades` 호환 입력은 `island`, `fromItem`, `reqAmount`, `toItem`, `count`, `yield`를 제공해야 한다. `fromTier == 0`의 `reqAmount`, `toTier == coin`의 `yield`, 그리고 `count`는 화면 관찰값이 필요하다.
- OCR 원문, 정규화 후보, 출처(`ocr`/`reference_rule`/`user_confirmed`)와 검수 사유를 함께 보존한다.

### FR-03 알려진 후보 매칭

- 원본 `masterData`의 1~7단 품목명과 `islandCoordinates`의 섬 이름을 후보 사전으로 사용한다.
- OCR 원문과 후보 간 유사도, 열 위치, 단계 표기, 수량 형식을 함께 사용한다.
- 확신도가 낮거나 후보 간 점수 차가 작으면 자동 확정하지 않고 `review_required`로 남긴다.
- 사용자는 해당 행만 수정하고 확정할 수 있어야 한다.

### FR-04 스크롤 수집과 중복 제거

- 사용자가 목록을 스크롤하는 동안 바뀐 프레임의 행을 수집한다.
- 같은 행이 여러 화면에 겹쳐 보여도 하나로 합친다.
- 기본 중복 판별 키는 `island + from_item + to_item + remaining_count`이며, OCR 확신도와 화면 위치는 보조 근거로 사용한다.
- 일반 실행에서는 캡처 이미지를 디스크에 계속 저장하지 않는다. 사용자가 켠 디버그 모드에서만 제한적으로 저장할 수 있다.

### FR-05 로컬 재고

- 물물교환 재고는 앱의 로컬 영구 저장소가 Source of Truth다.
- 최초 사용 시 사용자가 재고를 직접 입력할 수 있다.
- `0`은 확인된 0이며, 미입력/미확인은 `null`과 별도 입력 상태로 표현한다.
- 앱을 종료하고 다시 실행해도 확정 재고와 변경 이력이 유지되어야 한다.

### FR-06 스케줄 생성

- OCR PoC 통과 후 원본 v14.1의 핵심 계산을 별도 Scheduler 계층으로 이식한다.
- 일반 엔진은 쾌속/균형 계획, 재고 목표·보존량, 교섭력, 무게, 연쇄, 결핍, 길목, 거리·권역·소량·과적 페널티를 다룬다.
- 7단 엔진은 원본의 2지역/3지역 조합과 6→7단 연쇄를 재현한다.
- 먼저 동일 입력에서 동등한 결과를 재현하고, 개선은 별도 결정과 테스트 이후에 한다.

원본에서 확인한 교역 모드 7개는 다음과 같다.

1. 재고, 까마귀 주화 제외 (`none`)
2. 재고 + 내해 까마귀 주화 (`inner`)
3. 재고 + 대양 까마귀 주화 (`ocean`)
4. 7단 2지역, 그란디하 제외 (`t7_2region`)
5. 7단 2지역, 해모 제외 (`t7_2region_south`)
6. 7단 2지역, 아레하자 제외 (`t7_2region_arehazaX`)
7. 7단 3지역 (`t7_3region`)

사용자에게는 각 내부 키보다 이해하기 쉬운 한국어 설명을 우선 표시한다.

### FR-07 계획과 실제 완료 분리

- 계획 생성은 읽기 전용 계산이다.
- 계획의 수정·삭제·재계산도 재고를 바꾸지 않는다.
- 사용자가 실제 수행한 교환을 확인하고 완료를 명시적으로 확정해야 한다.
- 일부만 수행한 경우 수행 수량을 조정한 뒤 확정할 수 있어야 한다.
- 같은 완료 기록이 중복 적용되지 않아야 한다.

### FR-08 사용자 화면

최종 UI는 최소한 다음 영역을 제공한다.

- 스캔 시작/종료와 캡처 상태
- 인식한 교환 목록과 `검수 필요` 표시
- 재고 입력·조회
- 교섭력과 100%/170% 무게 설정
- 쾌속/균형 출항 계획
- 실제 수행 수량 확인과 완료 처리
- 재고 변경 미리보기와 변경 이력

구체 UI 프레임워크와 배치는 M7에서 정한다.

## 5. 비기능 요구사항

- 오프라인 핵심 동작: 설치에 필요한 파일 다운로드를 제외하면 실행 중 외부 OCR/AI 서비스가 필요 없어야 한다.
- 추적 가능성: OCR 원문, 정규화 결과, 확신도, 사용자 수정, 계획, 완료 기록을 구분한다.
- 복구 가능성: 로컬 상태 저장 실패 시 이전 정상 상태를 훼손하지 않는다.
- 멱등성: 같은 완료 작업을 재시도해도 재고가 이중 반영되지 않는다.
- 교체 가능성: OCR 엔진은 캡처·매칭·스케줄 계층과 분리해 교체할 수 있어야 한다.
- 테스트 가능성: 스케줄 계산과 재고 delta 계산은 UI와 분리된 결정적 함수로 검증한다.

## 6. 범위 밖

- 게임 메모리 읽기
- 패킷 분석 또는 변조
- 키보드·마우스 자동 입력
- 자동 항해·자동 교환·자동 플레이
- 게임 목록 자동 스크롤
- 외부 AI/OCR API
- Notion을 물물교환 재고의 실시간 저장소로 사용
- 원본 HTML UI의 그대로인 복제
- M0 단계의 Python 기능, OCR 설치, GUI 구현

## 7. 단계별 성공 조건

- M0: 문서, 빈 폴더 구조, 원본 자료가 준비되고 서로 모순이 없다.
- M1: 고정 샘플의 완전한 6개 행을 구조화하고 불확실 값을 안전하게 검수 대상으로 보낸다.
- M2: 게임 창의 보이는 client area를 메모리 캡처해 M1 pipeline에 전달한다.
- M3: coverage가 완전한 목록을 수집하고, reference-aware local parser가 원본 `scannedTrades` 호환 입력을 안전하게 생성한다. 일반 교환의 규칙 파생과 base/coin 예외 검수가 모두 검증되어야 전체 통과다.
- M4: 미확인과 0을 구분하는 영구 재고가 재시작 후에도 유지된다.
- M5: 고정 fixture에서 원본 v14.1과 동등한 계획을 재현한다.
- M6: 완료 확정 전에는 재고가 불변이고, 확정 후 정확한 delta가 한 번만 적용된다.
- M7~M8: 사용 흐름이 하나의 UI와 배포물에서 작동한다.

## 8. 미결정 사항

- 로컬 OCR 엔진과 모델
- 최초 1회 수동 영역 지정과 자동 창 탐지의 우선순위
- 로컬 저장 형식(SQLite/JSON 등)
- OCR 자동 확정 임계값과 후보 간 최소 점수 차
- 최종 GUI 프레임워크
- Windows 패키징 도구와 업데이트 방식

이 항목들은 앞선 마일스톤의 측정 결과 없이 확정하지 않는다.
